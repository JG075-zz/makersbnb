<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <!-- Bootstrap 3 CSS file -->
    <link href="/datetimepicker/dist/css/bootstrap.css" rel="stylesheet">
    <!-- Bootstrap Datetimepicker CSS file -->
    <link href="/datetimepicker/bootstrap-datetimepicker.min.css" rel="stylesheet">

    <style>
      body {
        margin: 0;
        font-family: 'Roboto', sans-serif;
      }
      .header {
        background: #49b2d2;
        color: #fff;
        font-size: 22px;
        padding-left: 40px;
        padding-top: 20px;
      }
      .container {
        padding: 40px;
      }
      .login-form {
        margin-top: 60px;
      }
      .login-form h1 {
        margin-bottom: 50px;
      }
      .col-centered{
        float: none;
        margin: 0 auto;
      }
      .main-logo {
        width:60%;
        margin-top: 100px;
      }
      .login-form .col-md-4 {
        margin-right: 640px;
      }
      .login-form .btn {
        color: #ffffff;
        background-color: #ff7977;
        border-color: #e97b79;
        margin-left: 20px;
        padding: 10px 40px;
      }
      .login-form a {
        color: #2DB25B;
        text-decoration: underline;
        margin-left: 12px
      }
      .login-form .col-sm-offset-6 {
        padding-top: 5px;
      }
      .logo .col-md-4 {
        text-align: center;
      }

    </style>
  </head>
  <body>
    <% if current_user %>
    <div class="row header">
          <div class="col-md-1">
            <h4>Welcome, <%= current_user.name %></h4>
          </div>
          <div class="col-md-2">
            <a class="btn btn-default" href="/spaces" role="button">Spaces</a>
            <a class="btn btn-default" href="/requests" role="button">Requests</a>
            <a class="btn btn-default" href="/spaces/mylistings" role="button">My Listings</a>

          </div>
          <div class="col-md-1 pull-right">
            <form action="/sessions" method="post">
              <input type="hidden" name="_method" value="delete">
              <button class="btn btn-default" type="submit">Log out</button>
            </form>
          </div>
        <% end %>
        <% if flash[:errors] %>
          <div id='errors'>
            <%= flash[:errors] %>
          </div>
        <% end %>
        <% if flash[:notice] %>
          <div id='notice'>
            <%= flash[:notice] %>
          </div>
        <% end %>
      </header>
      </div>
    </div>
    <%= yield %>

    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>

    <!-- Bootstrap 3 -->
    <script src="/datetimepicker/dist/js/bootstrap.min.js"></script>

    <!-- Moment.js -->
    <script src="/datetimepicker/moment.js"></script>

    <!-- Bootstrap Datetimepicker -->
    <script src="/datetimepicker/bootstrap-datetimepicker.min.js"></script>
    <!-- initialize the pickers -->
    <script type="text/javascript">
      $(function () {
        $('#start').datetimepicker();
        $('#stop').datetimepicker();
      });
    </script>

  </body>
</html>
